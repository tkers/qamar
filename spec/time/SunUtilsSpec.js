"use strict";

const SunUtils = require("../../lib/time/SunUtils");

describe("SunUtils", () => {

    it("should be exported", () => {
        expect(typeof SunUtils).toEqual("object");
    });

});
